# Release hardening

Treat release as evidence aggregation, not a label. Verify version consistency, complete deterministic tests, build/install from a clean wheel outside the source tree, inspect the diff and source status, then tag only after all configured release checks pass.
