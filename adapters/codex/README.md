# Codex Adapter

Status: planned for v0.1.

Responsibilities:

- generate Codex-compatible agent definitions from canonical roles
- resolve semantic compute profiles to current Codex model/reasoning settings
- preserve read/write intent and provider-specific safety controls
- expose validation that generated configuration matches the canonical source

Current Codex configuration details should be checked against current official documentation before implementation because the product surface evolves quickly.
